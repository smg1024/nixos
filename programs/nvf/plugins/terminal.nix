{
  # terminal
  toggleterm = {
    enable = true;
    setupOpts = {
      direction = "horizontal";
    };
    mappings = {
      open = "<leader>tt";
    };

    # lazygit
    lazygit = {
      enable = true;
      mappings = {
        open = "<leader>gg";
      };
    };
  };
}
